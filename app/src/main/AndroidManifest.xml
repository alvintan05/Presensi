<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pnj.presensi">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_presensi_app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_presensi_app"
        android:supportsRtl="true"
        android:theme="@style/Theme.Presensi"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.face_recognition.RecordFaceActivity" />
        <activity android:name=".ui.riwayat.DetailRiwayatActivity" />
        <activity android:name=".ui.ProfileActivity" />
        <activity android:name=".ui.PasswordActivity" />
        <activity android:name=".ui.face_recognition.FaceRecognitionActivity" />
        <activity android:name=".ui.AktivitasActivity" />
        <activity android:name=".ui.riwayat.RiwayatActivity" />
        <activity
            android:name=".ui.LoginActivity"
            android:theme="@style/NoActionBar" />
        <activity
            android:name=".ui.SplashScreenActivity"
            android:theme="@style/NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".ui.MapsActivity" />
        <activity
            android:name=".ui.HomeActivity"
            android:theme="@style/NoActionBar" />
    </application>

</manifest>